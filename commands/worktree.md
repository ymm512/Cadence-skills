# /worktree - 创建隔离开发环境

调用 `using-git-worktrees` skill 制定实现计划。

## 使用场景

快速创建隔离的 git worktree 开发环境，避免污染主分支。

## 相关命令

**前置命令**:
- `/plan` - 生成实现计划（可选）

**后续命令**:
- `/develop` - 在隔离环境中实现代码

## 详细文档

查看完整 Skill 文档: [using-git-worktrees](../skills/using-git-worktrees/SKILL.md)

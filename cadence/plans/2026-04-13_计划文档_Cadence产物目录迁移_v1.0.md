# Cadence Artifact Directory Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Cadence 维护的输出产物从 `.claude/` 迁移到 `cadence/`，并在 `rule-config` 中提供历史迁移与 `.gitignore` 用户确认能力。

**Architecture:** 本次是 Markdown/config 文档迁移，不新增运行时代码。先更新初始化规则与 `rule-config`，再更新 workflow commands/skills 的产物路径，最后更新面向用户的 README 示例，并用 `rg` 验证保留路径没有被误改。

**Tech Stack:** Markdown 文档、Claude Code command/skill 说明文件、ripgrep 验证

**Spec:** `.claude/prds/2026-04-13_概要需求_Cadence产物目录迁移_v1.0.md`

---

## File Map

- Modify: `cadence-init/commands/rule-config.md`
  - 负责创建 `.claude/rules` 与 `cadence/` 目录结构。
  - 新增历史产物迁移流程和 `cadence/` `.gitignore` 用户确认。
  - 将用户自定义规则说明从 `.claude/project-rules` 改为 `cadence/project-rules`。
- Modify: `cadence-init/commands/project-rules-examples.md`
  - 将项目规则示例输出位置迁到 `cadence/project-rules`。
- Modify: `cadence-init/commands/project-analysis.md`
  - 将项目初始化摘要分析报告输出位置迁到 `cadence/analysis-docs`。
- Modify: `cadence-init/references/rules/document-storage.md`
  - 更新新项目的文档存储规则模板。
- Modify: `cadence-init/references/rules/README.md`
  - 更新用户自定义规则目录说明，保留 `.claude/rules` 框架规则说明。
- Modify: `cadence-init/references/project-rules/**`
  - 更新项目规则模板和示例中的目录说明到 `cadence/project-rules`。
- Modify: `cadence-workflow/commands/*.md`
  - 更新命令输出路径说明到 `cadence/`。
- Modify: `cadence-workflow/skills/**/*.md`
  - 更新 Cadence 产物读写路径到 `cadence/`，保留 `.claude/skills`、`~/.claude/skills`、`.claude/rules`。
- Modify: `readmes/skills/*.md`
  - 更新面向用户的产物路径示例。
- Modify: `README.md`
  - 更新安装/初始化说明中用户规则目录为 `cadence/project-rules`。
- Do not modify: `.claude/rules/**`
  - 当前仓库内置规则文件按仓库约束不直接修改。
- Preserve references:
  - `.claude/rules`
  - `.claude/commands`
  - `.claude/skills`
  - `~/.claude/skills`
  - `.claude-plugin`

---

## Task 1: 更新 `rule-config` 初始化目录、规则摘要和迁移流程

**Files:**
- Modify: `cadence-init/commands/rule-config.md`

- [ ] **Step 1: 读取当前上下文**

Run:

```bash
sed -n '1,340p' cadence-init/commands/rule-config.md
```

Expected: 输出包含检查清单、CLAUDE.md 模板、目录结构创建和核心原则。

- [ ] **Step 2: 更新检查清单**

在检查清单中将第 5 项从“创建 `.claude/` 子目录”改成“创建 `.claude/rules` 与 `cadence/` 目录结构”，并新增“历史产物迁移”和“`cadence/` `.gitignore` 用户确认”两个步骤。

目标检查清单内容：

```markdown
1. **创建 rules 目录和规则文件** — 检测项目类型，定位模板目录，复制规则文件到 `.claude/rules/`
2. **添加 CLAUDE.md 规则引用** — 在 CLAUDE.md 中添加全部 8 条规则的摘要引用（规则 2 根据步骤 1a 检测结果选择对应文本）
3. **包管理器规则** — 前端使用 pnpm，Python 使用 uv
4. **技术栈检测** — 自动检测语言、测试/检查/格式化命令，需要用户确认
5. **目录结构创建** — 创建 `.claude/rules` 与 `cadence/` 产物目录
6. **历史产物迁移** — 检测旧 `.claude/` 产物目录，用户确认后迁移到 `cadence/`
7. **cadence gitignore 决策** — 询问用户是否将 `cadence/` 加入 `.gitignore`，默认不忽略
8. **Playwright Skills 规则配置** — 配置 Playwright CLI 的使用规则（可选）
```

- [ ] **Step 3: 更新 CLAUDE.md 模板中的用户规则引用**

将模板中这些文本：

```markdown
> 用户自定义规则见 `.claude/project-rules/` 目录。
- **用户自定义规则只能存放在 `.claude/project-rules/` 目录**
- 详见 `.claude/project-rules/README.md`
```

替换为：

```markdown
> 用户自定义规则见 `cadence/project-rules/` 目录。
- **用户自定义规则只能存放在 `cadence/project-rules/` 目录**
- 详见 `cadence/project-rules/README.md`
```

保留所有 `.claude/rules/...` 引用。

- [ ] **Step 4: 更新目录创建命令**

将原目录创建命令：

```bash
mkdir -p .claude/{rules,prds,analysis-docs,docs,designs,designs-reviews,plans,readmes,modaos,models,architecture,notes,logs,reports,project-rules/examples}
```

替换为：

```bash
mkdir -p .claude/rules
mkdir -p cadence/{prds,analysis,analysis-docs,docs,designs,designs-reviews,plans,readmes,modaos,models,architecture,notes,logs,reports,project-rules/examples,cache}
```

- [ ] **Step 5: 更新目录用途表**

将 `prds`、`analysis-docs`、`docs`、`designs`、`designs-reviews`、`plans`、`readmes`、`modaos`、`models`、`architecture`、`notes`、`logs`、`reports`、`project-rules` 的目录展示改为 `cadence/<name>/`。

保留：

```markdown
| `.claude/rules/` | 框架规则 | 内置规则文件（维护者管理） |
```

新增：

```markdown
| `cadence/analysis/` | 旧版分析报告 | 兼容旧版 Cadence analysis 输出目录 |
| `cadence/cache/` | 分析缓存 | @git-review 等流程生成的 Cadence 缓存 |
```

- [ ] **Step 6: 添加历史迁移流程**

在目录结构创建章节后、Playwright 章节前插入：

````markdown
### 6. 历史产物迁移

**检测旧目录**：

检查以下旧目录是否存在：

```bash
for dir in prds analysis analysis-docs docs designs designs-reviews plans readmes modaos models architecture notes logs reports project-rules cache; do
  test -e ".claude/$dir" && echo ".claude/$dir -> cadence/$dir"
done
```

**用户确认**：

- 如果没有检测到旧目录，提示无需迁移并继续。
- 如果检测到旧目录，展示迁移清单，并询问用户是否迁移到 `cadence/`。
- 默认建议迁移。

**迁移规则**：

| 场景 | 处理方式 |
| --- | --- |
| `cadence/<dir>` 不存在 | 将 `.claude/<dir>` 移动到 `cadence/<dir>` |
| `cadence/<dir>` 已存在且为空 | 将 `.claude/<dir>` 的内容移动到 `cadence/<dir>` |
| `cadence/<dir>` 已存在且非空 | 跳过该目录并报告冲突，要求用户手动处理 |

**禁止迁移**：

- `.claude/rules`
- `.claude/commands`
- `.claude/skills`

**迁移命令示例**：

```bash
mkdir -p cadence
for dir in prds analysis analysis-docs docs designs designs-reviews plans readmes modaos models architecture notes logs reports project-rules cache; do
  if [ -e ".claude/$dir" ]; then
    if [ ! -e "cadence/$dir" ]; then
      mv ".claude/$dir" "cadence/$dir"
    elif [ -d "cadence/$dir" ] && [ -z "$(find "cadence/$dir" -mindepth 1 -maxdepth 1 2>/dev/null)" ]; then
      find ".claude/$dir" -mindepth 1 -maxdepth 1 -exec mv {} "cadence/$dir/" \;
      rmdir ".claude/$dir" 2>/dev/null || true
    else
      echo "跳过冲突目录: .claude/$dir -> cadence/$dir"
    fi
  fi
done
```

**完成报告**：

迁移完成后，向用户报告已迁移目录、跳过目录和需要手动处理的冲突目录。
````

- [ ] **Step 7: 添加 `cadence/` `.gitignore` 决策流程**

在历史迁移流程后、Playwright 章节前插入：

````markdown
### 7. cadence gitignore 决策

**目的**：让用户决定是否将 `cadence/` 作为本地工作目录忽略。

**用户确认**：

- 询问用户：“是否将 `cadence/` 加入 `.gitignore`？”
- 默认选择：不忽略。
- 默认不忽略的原因：PRD、设计、计划、用户项目规则等产物通常需要团队协作和版本管理。

**如果用户选择忽略**：

检查 `.gitignore` 是否已包含 `cadence/`：

```bash
grep -qxF 'cadence/' .gitignore 2>/dev/null || printf '\n# Cadence 产物目录\ncadence/\n' >> .gitignore
```

**如果用户选择不忽略**：

不修改 `.gitignore`。
````

- [ ] **Step 8: 更新章节编号和核心原则**

将 Playwright 章节编号从 `### 6.` 改为 `### 8.`。

将核心原则中的：

```markdown
- **规则分离** — 框架规则放 `.claude/rules/`，用户规则放 `.claude/project-rules/`
- **目录明确** — 每种文档类型有明确存储位置
```

替换为：

```markdown
- **规则分离** — 框架规则放 `.claude/rules/`，用户规则放 `cadence/project-rules/`
- **目录明确** — Claude Code 配置保留在 `.claude/`，Cadence 产物放在 `cadence/`
```

- [ ] **Step 9: 验证 `rule-config`**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|cache|project-rules)" cadence-init/commands/rule-config.md
rg -n "cadence/|历史产物迁移|gitignore" cadence-init/commands/rule-config.md
```

Expected:

- 第一条命令只允许剩余 `.claude/project-rules` 的旧路径出现在“历史迁移”检测清单或明确说明旧路径的地方。
- 第二条命令能看到 `cadence/` 目录创建、历史迁移、`.gitignore` 默认不忽略说明。

- [ ] **Step 10: Commit**

```bash
git add cadence-init/commands/rule-config.md
git commit -m "feat: add cadence artifact migration to rule-config"
```

---

## Task 2: 更新初始化模板与 project-rules 命令

**Files:**
- Modify: `cadence-init/references/rules/document-storage.md`
- Modify: `cadence-init/references/rules/README.md`
- Modify: `cadence-init/references/project-rules/README.md`
- Modify: `cadence-init/references/project-rules/CLAUDE-RULE.md`
- Modify: `cadence-init/references/project-rules/examples/coding-standards.md`
- Modify: `cadence-init/references/project-rules/examples/requirement-template.md`
- Modify: `cadence-init/references/project-rules/examples/test-standards.md`
- Modify: `cadence-init/commands/project-rules-examples.md`
- Modify: `cadence-init/commands/project-analysis.md`

- [ ] **Step 1: 更新 document-storage 模板**

在 `cadence-init/references/rules/document-storage.md` 中执行这些语义替换：

```text
.claude/plans/ -> cadence/plans/
.claude/prds/ -> cadence/prds/
.claude/docs/ -> cadence/docs/
.claude/designs/ -> cadence/designs/
.claude/designs-reviews/ -> cadence/designs-reviews/
.claude/analysis-docs/ -> cadence/analysis-docs/
.claude/reports/ -> cadence/reports/
.claude/project-rules/ -> cadence/project-rules/
.claude/modaos/ -> cadence/modaos/
.claude/models/ -> cadence/models/
.claude/architecture/ -> cadence/architecture/
.claude/notes/ -> cadence/notes/
.claude/logs/ -> cadence/logs/
.claude/readmes/ -> cadence/readmes/
```

保留 `.claude/rules/`。

同时将总原则改为：

```markdown
> **Cadence 产物文档必须存放在 `cadence` 目录下；Claude Code 框架规则保留在 `.claude/rules` 目录下。**
```

- [ ] **Step 2: 更新 README 类路径说明**

在 `cadence-init/references/rules/document-storage.md` 中，将开发相关 README 目录从 `.claude/readmes/` 改为 `cadence/readmes/`；保留根目录 `README.md` 和根目录 `readmes/` 的项目介绍类例外。

- [ ] **Step 3: 更新 project-rules 模板文件**

在以下文件中，将用户自定义规则路径从 `.claude/project-rules/` 改为 `cadence/project-rules/`：

```text
cadence-init/references/project-rules/README.md
cadence-init/references/project-rules/CLAUDE-RULE.md
cadence-init/references/project-rules/examples/coding-standards.md
cadence-init/references/project-rules/examples/requirement-template.md
cadence-init/references/project-rules/examples/test-standards.md
```

保留 `.claude/rules/` 的框架规则说明。

- [ ] **Step 4: 更新 project-rules-examples command**

在 `cadence-init/commands/project-rules-examples.md` 中将输出路径改为：

```markdown
cadence/project-rules/README.md
cadence/project-rules/examples/requirement-template.md
cadence/project-rules/examples/design-template.md
cadence/project-rules/examples/coding-standards.md
cadence/project-rules/examples/test-standards.md
```

将“禁止将用户自定义规则写入 `.claude/rules/` 目录”保留不变。

- [ ] **Step 5: 更新 project-analysis command**

在 `cadence-init/commands/project-analysis.md` 中，将输出文件路径改为：

```markdown
cadence/analysis-docs/YYYY-MM-DD_分析报告_项目初始化摘要_v1.0.md
```

- [ ] **Step 6: 验证初始化模板**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|readmes|project-rules)" cadence-init/references cadence-init/commands/project-rules-examples.md cadence-init/commands/project-analysis.md
```

Expected:

- 不应出现旧的 Cadence 产物输出路径。
- `.claude/rules` 不在此搜索范围内，不需要消除。

- [ ] **Step 7: Commit**

```bash
git add cadence-init/references/rules/document-storage.md cadence-init/references/rules/README.md cadence-init/references/project-rules cadence-init/commands/project-rules-examples.md cadence-init/commands/project-analysis.md
git commit -m "docs: move cadence init artifact paths to cadence directory"
```

---

## Task 3: 更新 workflow commands 的输出路径

**Files:**
- Modify: `cadence-workflow/commands/design.md`
- Modify: `cadence-workflow/commands/plan.md`
- Modify: `cadence-workflow/commands/full-flow.md`
- Modify: `cadence-workflow/commands/design-review.md`
- Modify: `cadence-workflow/commands/quick-flow.md`

- [ ] **Step 1: 更新命令文件中的产物路径**

执行这些语义替换：

```text
.claude/docs/ -> cadence/docs/
.claude/designs/ -> cadence/designs/
.claude/plans/ -> cadence/plans/
.claude/analysis/ -> cadence/analysis/
.claude/analysis-docs/ -> cadence/analysis-docs/
.claude/designs-reviews/ -> cadence/designs-reviews/
.claude/reports/ -> cadence/reports/
.claude/prds/ -> cadence/prds/
```

- [ ] **Step 2: 验证 commands**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|cache|project-rules)" cadence-workflow/commands
```

Expected: 无输出。

- [ ] **Step 3: Commit**

```bash
git add cadence-workflow/commands
git commit -m "docs: move workflow command outputs to cadence directory"
```

---

## Task 4: 更新 workflow skills 的产物路径

**Files:**
- Modify: `cadence-workflow/skills/analyze/SKILL.md`
- Modify: `cadence-workflow/skills/brainstorming/SKILL.md`
- Modify: `cadence-workflow/skills/checkpoint/SKILL.md`
- Modify: `cadence-workflow/skills/design/SKILL.md`
- Modify: `cadence-workflow/skills/design-review/SKILL.md`
- Modify: `cadence-workflow/skills/exploration-flow/SKILL.md`
- Modify: `cadence-workflow/skills/full-flow/SKILL.md`
- Modify: `cadence-workflow/skills/git-review/README.md`
- Modify: `cadence-workflow/skills/git-review/analyzers/ai-prompts.md`
- Modify: `cadence-workflow/skills/lock-utils/SKILL.md`
- Modify: `cadence-workflow/skills/plan/SKILL.md`
- Modify: `cadence-workflow/skills/quick-flow/SKILL.md`
- Modify: `cadence-workflow/skills/report/SKILL.md`
- Modify: `cadence-workflow/skills/requirement/SKILL.md`
- Modify: `cadence-workflow/skills/subagent-development/SKILL.md`
- Modify: `cadence-workflow/skills/transaction-utils/SKILL.md`

- [ ] **Step 1: 更新产物路径**

在上述文件中执行这些语义替换：

```text
.claude/docs/ -> cadence/docs/
.claude/designs/ -> cadence/designs/
.claude/plans/ -> cadence/plans/
.claude/analysis/ -> cadence/analysis/
.claude/analysis-docs/ -> cadence/analysis-docs/
.claude/designs-reviews/ -> cadence/designs-reviews/
.claude/reports/ -> cadence/reports/
.claude/prds/ -> cadence/prds/
.claude/cache/ -> cadence/cache/
```

同时将无尾斜杠形式改掉：

```text
.claude/docs -> cadence/docs
.claude/designs -> cadence/designs
.claude/plans -> cadence/plans
.claude/analysis -> cadence/analysis
.claude/analysis-docs -> cadence/analysis-docs
.claude/designs-reviews -> cadence/designs-reviews
.claude/reports -> cadence/reports
.claude/prds -> cadence/prds
.claude/cache -> cadence/cache
```

- [ ] **Step 2: 保留 Claude Code 专属路径**

不要修改这些引用：

```text
.claude/CLAUDE.md
.claude/rules/
.claude/commands/
.claude/skills/
~/.claude/skills/
```

- [ ] **Step 3: 验证 skills**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|cache|project-rules)" cadence-workflow/skills
rg -n "\.claude/(rules|commands|skills)|~/.claude/skills" cadence-workflow/skills
```

Expected:

- 第一条命令无输出。
- 第二条命令可以有输出，且只表示 Claude Code 保留路径或 skill 安装目标。

- [ ] **Step 4: Commit**

```bash
git add cadence-workflow/skills
git commit -m "docs: move workflow skill artifacts to cadence directory"
```

---

## Task 5: 更新用户 README 示例与仓库入口说明

**Files:**
- Modify: `readmes/skills/full-flow.md`
- Modify: `readmes/skills/checkpoint.md`
- Modify: `readmes/skills/brainstorming.md`
- Modify: `readmes/skills/exploration-flow.md`
- Modify: `readmes/skills/report.md`
- Modify: `readmes/skills/quick-flow.md`
- Modify: `README.md`
- Modify: `CLAUDE.md`
- Modify: `AGENTS.md`

- [ ] **Step 1: 更新 readmes 中的 Cadence 产物路径**

在 `readmes/skills/` 中执行这些语义替换：

```text
.claude/docs/ -> cadence/docs/
.claude/designs/ -> cadence/designs/
.claude/plans/ -> cadence/plans/
.claude/analysis/ -> cadence/analysis/
.claude/reports/ -> cadence/reports/
.claude/prds/ -> cadence/prds/
```

保留 `readmes/skills/skill-creator.md` 中 `.claude/skills` 和 `~/.claude/skills`。

- [ ] **Step 2: 更新 README 初始化说明**

在 `README.md` 中将：

```markdown
- ✅ 创建 `.claude/project-rules/` 用户规则目录
```

替换为：

```markdown
- ✅ 创建 `cadence/project-rules/` 用户规则目录
```

保留 `.claude/rules/` 规则目录说明。

- [ ] **Step 3: 更新 AGENTS.md 与 CLAUDE.md 的用户规则摘要**

在 `AGENTS.md` 和 `CLAUDE.md` 中将用户自定义规则目录从 `.claude/project-rules/` 改为 `cadence/project-rules/`。

保留所有 `.claude/rules/...` 引用。

- [ ] **Step 4: 验证 README 和入口说明**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|cache|project-rules)" readmes README.md AGENTS.md CLAUDE.md
rg -n "\.claude/(rules|skills)|~/.claude/skills" readmes README.md AGENTS.md CLAUDE.md
```

Expected:

- 第一条命令无输出。
- 第二条命令可以有输出，且只表示保留的规则或 skill 安装路径。

- [ ] **Step 5: Commit**

```bash
git add readmes README.md AGENTS.md CLAUDE.md
git commit -m "docs: update user docs for cadence artifact directory"
```

---

## Task 6: 全局验证与收尾

**Files:**
- Verify: all modified files

- [ ] **Step 1: 检查旧 Cadence 产物路径残留**

Run:

```bash
rg -n "\.claude/(prds|plans|designs|designs-reviews|analysis|analysis-docs|reports|docs|logs|notes|models|architecture|modaos|cache|project-rules)" cadence-workflow cadence-init readmes README.md AGENTS.md CLAUDE.md
```

Expected: 只允许在 `rule-config` 的历史迁移章节和 spec/plan 文档中作为“旧路径”出现；本命令未包含 spec/plan，实际应只剩 `rule-config` 历史迁移章节。

- [ ] **Step 2: 检查保留路径仍存在**

Run:

```bash
rg -n "\.claude/(rules|commands|skills)|~/.claude/skills|\.claude-plugin" cadence-workflow cadence-init readmes README.md AGENTS.md CLAUDE.md
```

Expected: 有输出，表示 `.claude/rules`、`.claude/skills`、`~/.claude/skills`、`.claude-plugin` 等保留路径没有被误删。

- [ ] **Step 3: 检查 `skill-create` 和 `skill-creator` 保持 `.claude/skills`**

Run:

```bash
rg -n "\.claude/skills|~/.claude/skills" cadence-init/commands/skill-create.md cadence-init/skills/skill-creator readmes/skills/skill-creator.md
```

Expected: 有输出，且 project skill 仍指向 `.claude/skills/<skill-name>/SKILL.md`。

- [ ] **Step 4: 检查 rule-config 新能力**

Run:

```bash
rg -n "历史产物迁移|cadence gitignore|默认.*不忽略|cadence/\{prds" cadence-init/commands/rule-config.md
```

Expected: 能看到历史迁移章节、`.gitignore` 默认不忽略说明和 `cadence/` 目录创建命令。

- [ ] **Step 5: 检查 Markdown 格式风险**

Run:

```bash
rg -n "````|```" cadence-init/commands/rule-config.md cadence-init/references/rules/document-storage.md cadence-workflow/skills/brainstorming/SKILL.md
```

Expected: 嵌套代码块位置使用四反引号包裹外层内容，普通代码块可继续使用三反引号。

- [ ] **Step 6: 检查工作区**

Run:

```bash
git status --short
git log --oneline -6
```

Expected: 工作区干净；最近提交包含本计划中的每个任务提交。

- [ ] **Step 7: 汇报测试结果**

向用户汇报：

```text
测试结果：
- 旧 Cadence 产物路径残留检查：通过/失败，并列出残留。
- 保留路径检查：通过/失败，并列出保留路径。
- skill-create/skill-creator 检查：通过/失败。
- rule-config 新能力检查：通过/失败。
- Markdown 格式检查：通过/失败。
```

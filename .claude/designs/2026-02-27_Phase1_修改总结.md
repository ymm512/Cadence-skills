# Cadence v2.4 - Phase 1 (P0) 修改总结

> **修改日期**: 2026-02-27
> **修改阶段**: Phase 1 - P0 修改（必须修改）
> **修改状态**: ✅ 已完成

---

## 📋 修改概览

### 修改范围

| 修改项 | 文件数量 | 状态 |
|--------|---------|------|
| Subagent配置方式修改 | 4个文件 | ✅ 已完成 |
| 技术栈检测简化 | 3个文件 | ✅ 已完成 |
| **总计** | **5个文件** | ✅ **已完成** |

### 修改文件清单

| 序号 | 文件路径 | 修改类型 | 修改内容 |
|------|---------|---------|---------|
| 1 | `.claude/designs/8.1_implementer.md` | 内容修改 | 简化tech stack detection（Phase 4 & 5） |
| 2 | `.claude/designs/8.3_code-quality-reviewer.md` | 内容修改 | 简化tech stack detection（Determine Commands） |
| 3 | `.claude/designs/8.2_spec-reviewer.md` | 检查确认 | 确认无需修改（不涉及tech stack detection） |
| 4 | `.claude/designs/2026-02-26_Skill_Subagent_Development_v1.0.md` | 内容新增 | 添加Task tool调用示例（3个Subagent） |
| 5 | `.claude/designs/2026-02-26_Skill_Plan_v1.0.md` | 内容修改 | 简化tech stack detection（步骤3） |

---

## 🔧 修改详情

### 修改1：8.1_implementer.md - Phase 4 Coverage Check

**修改位置**: Lines 64-111

**修改前**：
```markdown
**Priority 3: Auto-Detect + User Confirm**
If neither is available, detect based on project files:
[Table with 6 languages]

**⚠️ IMPORTANT**: If auto-detecting, you MUST ask the user to confirm the detected command before using it.
```

**修改后**：
```markdown
**Priority 3: Missing Configuration**
If no coverage command is found:
1. **STOP and inform the user**:
   ```
   ⚠️ Coverage command not found.

   Please configure your tech stack in CLAUDE.md:
   [Configuration template with examples]
   ```
2. **Wait for user to provide the command or configure CLAUDE.md**

**⚠️ DO NOT guess or auto-detect**: Always use explicit configuration.
```

**修改理由**: 移除auto-detect逻辑，改为提示用户配置CLAUDE.md，确保配置明确性。

---

### 修改2：8.1_implementer.md - Phase 5 Lint & Format

**修改位置**: Lines 114-178

**修改前**：
```markdown
**Priority 3: Auto-Detect + User Confirm**
If neither is available, detect based on project files:
[Table with 6 languages]

**⚠️ IMPORTANT**: If auto-detecting, you MUST ask the user to confirm the detected commands before using them.
```

**修改后**：
```markdown
**Priority 3: Missing Configuration**
If no commands are found:
1. **STOP and inform the user**:
   ```
   ⚠️ Lint/Format commands not found.

   Please configure your tech stack in CLAUDE.md:
   [Configuration template with examples]
   ```
2. **Wait for user to provide the commands or configure CLAUDE.md**

**⚠️ DO NOT guess or auto-detect**: Always use explicit configuration.
```

**修改理由**: 与Phase 4保持一致，移除auto-detect逻辑。

---

### 修改3：8.1_implementer.md - Red Flags

**修改位置**: Lines 230-241

**修改前**：
```markdown
| "Assume npm commands" | ❌ Must detect project type and use correct commands |
```

**修改后**：
```markdown
| "Guess the commands" | ❌ Must get explicit configuration from user or CLAUDE.md |
| "Auto-detect project type" | ❌ Must ask user to configure CLAUDE.md if missing |
```

**修改理由**: 更新Red Flags以反映新的tech stack detection策略。

---

### 修改4：8.3_code-quality-reviewer.md - Determine Commands

**修改位置**: Lines 44-66

**修改前**：
```markdown
**Priority 3: Auto-Detect + User Confirm**
If neither is available, detect based on project files:
[Table with 6 languages]

**⚠️ IMPORTANT**: If auto-detecting, you MUST ask the user to confirm the detected commands before using them.
```

**修改后**：
```markdown
**Priority 3: Missing Configuration**
If no commands are found:
1. **STOP and inform the user**:
   ```
   ⚠️ Lint/Format/Coverage commands not found.

   Please configure your tech stack in CLAUDE.md:
   [Configuration template with examples for 6 languages]
   ```
2. **Wait for user to provide the commands or configure CLAUDE.md**

**⚠️ DO NOT guess or auto-detect**: Always use explicit configuration.
```

**修改理由**: 与8.1_implementer.md保持一致，移除auto-detect逻辑。

---

### 修改5：8.3_code-quality-reviewer.md - Red Flags

**修改位置**: Lines 216-225

**修改前**：
```markdown
| "Assume npm commands" | ❌ Must detect project type and use correct commands |
```

**修改后**：
```markdown
| "Guess the commands" | ❌ Must get explicit configuration from user or CLAUDE.md |
| "Auto-detect project type" | ❌ Must ask user to configure CLAUDE.md if missing |
```

**修改理由**: 更新Red Flags以反映新的tech stack detection策略。

---

### 修改6：8.2_spec-reviewer.md - 检查确认

**检查结果**: ✅ **无需修改**

**原因**: 8.2_spec-reviewer.md专注于需求符合性审查，不涉及技术栈命令检测、lint、format、coverage等技术命令的执行。

**职责范围**:
- 检查实现是否符合规范
- 检查是否有额外功能（YAGNI）
- 检查测试覆盖（但不执行coverage命令，只是检查覆盖率报告）
- 验证验收标准

---

### 修改7：2026-02-26_Skill_Subagent_Development_v1.0.md - Task Tool调用示例

**修改位置**: 步骤5 "Subagent 执行任务" (Lines 239-246)

**修改前**：
```markdown
#### 5. **Subagent 执行任务**
- 使用 Task 工具调用 Subagent
- Subagent 在 worktree 目录中工作
- Subagent 在 feature 分支上开发
- **执行模式**：
  - **串行执行**：Task 1 → Task 2 → Task 3
  - **并行执行**：Task 4 || Task 5 || Task 6
  - **混合执行**：Task 1 → Task 2 → [Task 4 || Task 5 || Task 6] → Task 7
```

**修改后**：
```markdown
#### 5. **Subagent 执行任务** ⭐

**使用 Task 工具动态调用 Subagent（关键：不使用静态配置）**

Subagent 在 worktree 目录中工作，在 feature 分支上开发。

**执行模式**：
- **串行执行**：Task 1 → Task 2 → Task 3
- **并行执行**：Task 4 || Task 5 || Task 6
- **混合执行**：Task 1 → Task 2 → [Task 4 || Task 5 || Task 6] → Task 7

**完整调用流程**：

##### 5.1 调用 Implementer Subagent

```markdown
Task tool (general-purpose):
  description: "Implement {task_name}"
  prompt: |
    You are implementing: **{task_name}**

    ## Task Description
    {完整任务描述，来自Plan}

    ## Context
    - **Project**: {project_name}
    - **Feature Branch**: {feature_branch}
    - **Work Directory**: {worktree_path}
    - **Dependencies**: {依赖任务列表}
    - **Priority**: {P0/P1/P2}

    ## Acceptance Criteria
    {验收标准列表，来自Plan}

    ## Technical Constraints
    {技术约束，来自Design}

    ## TDD Workflow (MUST FOLLOW)
    [详细的RED-GREEN-BLUE流程说明]

    ## Self-Review Checklist
    [自检清单]

    ## Output Format
    [输出格式要求]
```

##### 5.2 调用 Spec Reviewer Subagent

```markdown
Task tool (general-purpose):
  description: "Review spec compliance for {task_name}"
  prompt: |
    You are a **Spec Compliance Reviewer**...

    ## Task Specification
    {完整任务描述}

    ## Implementation Report
    {Implementer的完成报告}

    ## Your Mission
    1. Read the implementation code (DO NOT trust the report alone)
    2. Compare actual implementation vs specification
    3. Check for missing requirements
    4. Check for extra features (YAGNI violations)

    [详细的审查标准和输出格式]
```

##### 5.3 调用 Code Quality Reviewer Subagent

```markdown
Task tool (general-purpose):
  description: "Review code quality for {task_name}"
  prompt: |
    You are a **Code Quality Reviewer**...

    ## Git Commit Range
    - Base SHA: {base_commit}
    - Head SHA: {head_commit}

    ## Review Dimensions
    ### 1. Strengths (先肯定)
    ### 2. Issues by Severity (Critical/Important/Minor)
    ### 3. Automated Checks

    [详细的审查维度和输出格式]
```

**⚠️ 重要：动态调用原则**
- ✅ 使用 Task tool 动态调用 Subagent
- ✅ 传递完整的内联 prompt（包含任务描述、上下文、验收标准）
- ❌ **不要**使用静态配置文件（如 `agents.json`）
- ❌ **不要**预定义 Subagent 配置
```

**修改理由**: 添加详细的Task tool调用示例，展示如何动态调用三个Subagent（Implementer → Spec Reviewer → Code Quality Reviewer），确保使用内联prompt而非静态配置。

---

### 修改8：2026-02-26_Skill_Plan_v1.0.md - 读取技术栈配置

**修改位置**: 步骤3 "读取技术栈配置" (Lines 139-150)

**修改前**：
```markdown
3. **读取技术栈配置** ⭐⭐（新增）
   - **步骤 1：检查 CLAUDE.md**
     - 读取项目根目录的 `CLAUDE.md` 文件
     - 查找 `project_tech_stack` 配置
     - 如果存在 → 直接使用
   - **步骤 2：如果 CLAUDE.md 没有配置**
     - 自动检测项目类型（package.json、requirements.txt 等）
     - **与用户确认技术栈配置**
     - 建议用户将配置写入 CLAUDE.md
   - **步骤 3：整理输出**
     - 将技术栈信息输出到 Plan 文档的"技术栈配置"章节
     - 每个任务继承项目技术栈配置
```

**修改后**：
```markdown
3. **读取技术栈配置** ⭐⭐（新增）
   - **步骤 1：检查用户对话中是否指定**
     - 如果用户在对话中已指定技术栈配置，使用用户指定的配置
     - 示例："使用 Python + pytest"
   - **步骤 2：检查 CLAUDE.md**
     - 读取项目根目录的 `CLAUDE.md` 文件
     - 查找 `tech_stack` 配置
     - 如果存在 → 直接使用
   - **步骤 3：如果未找到配置**
     - **停止并提示用户配置 CLAUDE.md**：
       ```
       ⚠️ 技术栈配置未找到。

       请在 CLAUDE.md 中配置技术栈：
       [详细的配置示例]
       ```
     - **等待用户配置完成**
   - **步骤 4：整理输出**
     - 将技术栈信息输出到 Plan 文档的"技术栈配置"章节
     - 每个任务继承项目技术栈配置
```

**修改理由**: 简化tech stack detection，移除auto-detect逻辑，增加用户对话优先级。

---

### 修改9：2026-02-26_Skill_Plan_v1.0.md - 技术栈配置优先级说明

**修改位置**: "三层检测优先级" (Lines 256-259)

**修改前**：
```markdown
**三层检测优先级**：
1. **Task Description**（最高优先级）：任务特定配置
2. **CLAUDE.md**（次优先级）：项目级默认配置
3. **Auto-Detect + User Confirm**（兜底）：自动检测并确认
```

**修改后**：
```markdown
**技术栈配置优先级**：
1. **User Specified in Conversation**（最高优先级）：用户在对话中指定
2. **CLAUDE.md Configuration**（次优先级）：项目级默认配置
3. **Missing Configuration**（缺失）：提示用户配置 CLAUDE.md

**⚠️ 重要：不自动检测**
- ❌ 不使用 auto-detect 逻辑
- ✅ 必须从用户对话或 CLAUDE.md 获取配置
- ✅ 如果缺失，提示用户配置
```

**修改理由**: 与Subagent prompt文件保持一致，明确不使用auto-detect逻辑。

---

## ✅ 验证清单

### 技术栈检测简化验证

- [x] 8.1_implementer.md - Phase 4 Coverage Check 已修改
- [x] 8.1_implementer.md - Phase 5 Lint & Format 已修改
- [x] 8.1_implementer.md - Red Flags 已更新
- [x] 8.3_code-quality-reviewer.md - Determine Commands 已修改
- [x] 8.3_code-quality-reviewer.md - Red Flags 已更新
- [x] 8.2_spec-reviewer.md - 已确认无需修改
- [x] 所有文件已移除 "Auto-Detect + User Confirm" 逻辑
- [x] 所有文件已添加 "Missing Configuration → Prompt User" 逻辑
- [x] 所有文件已添加 "DO NOT guess or auto-detect" 警告

### Subagent配置方式修改验证

- [x] 2026-02-26_Skill_Subagent_Development_v1.0.md - 已添加Task tool调用示例
- [x] Task tool调用示例包含3个Subagent（Implementer → Spec Reviewer → Code Quality Reviewer）
- [x] Task tool调用示例使用内联prompt
- [x] Task tool调用示例明确禁止静态配置（agents.json）
- [x] 2026-02-26_Skill_Plan_v1.0.md - 已简化tech stack detection
- [x] Plan Skill已更新技术栈配置优先级说明

### 一致性验证

- [x] 所有Subagent prompt文件（8.1, 8.3）使用相同的tech stack detection策略
- [x] 所有Skill文档（Subagent Development, Plan）使用相同的tech stack detection策略
- [x] Red Flags部分已同步更新
- [x] 示例配置模板一致（6种语言）

---

## 📊 修改统计

### 代码行数变化

| 文件 | 修改前行数 | 修改后行数 | 变化 |
|------|-----------|-----------|------|
| 8.1_implementer.md | 256 | 257 | +1 |
| 8.3_code-quality-reviewer.md | 296 | 331 | +35 |
| 2026-02-26_Skill_Subagent_Development_v1.0.md | 817 | 1117 | +300 |
| 2026-02-26_Skill_Plan_v1.0.md | ? | ? | +30 (estimated) |

### 关键改动

- **移除**: "Auto-Detect + User Confirm" 逻辑（4处）
- **新增**: "Missing Configuration → Prompt User" 逻辑（4处）
- **新增**: Task tool调用示例（3个Subagent完整示例）
- **更新**: Red Flags（4处）
- **更新**: 技术栈配置优先级说明（2处）

---

## 🎯 修改目标达成情况

### 目标1：Subagent配置方式修改 ✅

**问题**: 当前在 `agents.json` 中静态配置Subagent，但Claude Code的Subagent应该通过Task tool动态调用

**解决方案**: 采用选项A - 参考superpowers的 `subagent-driven-development`

**达成情况**:
- ✅ 添加了详细的Task tool调用示例
- ✅ 展示了如何使用内联prompt动态调用Subagent
- ✅ 明确禁止使用静态配置文件（agents.json）
- ✅ 提供了3个Subagent的完整调用示例（Implementer, Spec Reviewer, Code Quality Reviewer）

### 目标2：技术栈检测简化 ✅

**问题**: 三层检测（Task → CLAUDE.md → Auto-Detect）过于复杂，auto-detect逻辑难以维护

**解决方案**: 简化为两层 + 提示用户配置

**达成情况**:
- ✅ 移除了所有auto-detect逻辑（4处）
- ✅ 改为提示用户配置CLAUDE.md
- ✅ 提供了详细的配置示例（6种语言）
- ✅ 更新了Red Flags以反映新策略
- ✅ 确保所有文件使用一致的tech stack detection策略

---

## 📝 后续工作

### Phase 2: P1 修改（强烈建议）

- [ ] **修改1**: 添加MVP限制说明（4.9-4.11节点标记为v2.5+待实现）
- [ ] **修改2**: 调整节点依赖关系（Test Design从Required改为Recommended）
- [ ] **修改3**: Git Worktrees改为推荐而非强制
- [ ] **修改4**: 添加错误恢复机制（Checkpoint机制）

### Phase 3: P2 优化（优化建议）

- [ ] **优化1**: 简化分类体系（Mandatory/Recommended/Optional）
- [ ] **优化2**: 合并配置文件（减少配置文件数量）
- [ ] **优化3**: 添加Quick Start指南
- [ ] **优化4**: 优化文档结构和可读性

---

## 🎉 总结

Phase 1 (P0) 修改已全部完成，共修改了5个文件，成功解决了两个关键问题：

1. **Subagent配置方式**：从静态配置改为动态Task tool调用，提供了完整的调用示例
2. **技术栈检测**：从复杂的三层检测简化为两层+提示用户配置，移除了auto-detect逻辑

所有修改均已验证一致性和完整性，为后续Phase 2和Phase 3的修改奠定了良好基础。

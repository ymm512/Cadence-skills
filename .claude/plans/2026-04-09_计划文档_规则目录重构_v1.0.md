# 规则目录重构实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 CLAUDE.md 中的详细规则拆分到 `.claude/rules/` 目录，新增 `references/rules/` 模板目录，更新所有相关 commands/skills 遵循新的规则存储规范。

**Architecture:** 双目录设计 — `rules/`（框架内置规则，维护者管理）+ `project-rules/`（用户自定义规则，用户管理）。CLAUDE.md 精简为摘要+引用。新增 `cadence-init/references/rules/` 作为规则内容模板源。

**Tech Stack:** Markdown 文档编辑，无代码。

**Spec:** `.claude/designs/2026-04-09_方案设计_规则目录重构_v1.0.md`

---

## Chunk 1: 创建 references/rules/ 模板文件

> 将 CLAUDE.md 中的规则内容原样拆分为 8 个独立的模板文件。

### Task 1: 创建 references/rules/ 目录和 README.md

**Files:**
- Create: `cadence-init/references/rules/README.md`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p cadence-init/references/rules
```

- [ ] **Step 2: 创建 README.md**

写入以下内容：

````markdown
# 框架内置规则目录

## 目录说明

本目录存放 Cadence 框架的内置规则文件。这些规则由框架维护者管理，在项目初始化时自动创建到用户项目的 `.claude/rules/` 目录。

## 文件列表

| 文件 | 内容概述 |
|------|---------|
| `language.md` | 语言规则（中文回答要求） |
| `code-usage.md` | 代码使用规则（非必要不编写代码） |
| `document-storage.md` | 文档存储规则（目录、命名、路径映射） |
| `markdown-format.md` | Markdown 格式规则（代码块嵌套） |
| `serena-usage.md` | Serena MCP 使用规则 |
| `mcp-servers.md` | MCP Server 使用规则（所有 MCP 工具） |
| `playwright.md` | Playwright CLI 使用规则 |

## 修改权限

- **仅框架维护者**可以修改本目录下的文件
- 用户自定义规则应放在 `.claude/project-rules/` 目录
- **禁止**用户直接修改 `.claude/rules/` 目录下的文件

## 从旧版迁移

如果你之前使用旧版 cadence-init 初始化了项目（CLAUDE.md 中包含完整规则文本），可以：

1. 重新运行 `/cadence:init:rule-config` 命令
2. 命令将自动创建 `.claude/rules/` 目录并写入规则文件
3. 手动将 CLAUDE.md 中的规则文本替换为摘要引用

## 相关目录

- 用户自定义规则：`.claude/project-rules/`
- 项目主配置：`CLAUDE.md`
````

- [ ] **Step 3: Commit**

```bash
git add cadence-init/references/rules/README.md
git commit -m "feat: 创建 references/rules 目录和 README 模板"
```

---

### Task 2: 创建 language.md 模板

**Files:**
- Create: `cadence-init/references/rules/language.md`

- [ ] **Step 1: 创建文件**

从当前 CLAUDE.md 第 9-11 行提取内容，写入：

```markdown
## 语言规则

- **必须使用中文回答** - 所有响应、解释、注释和文档必须使用中文。代码本身可以使用英文（变量名、函数名等），但所有与用户的交互必须使用中文。
```

- [ ] **Step 2: Commit**

```bash
git add cadence-init/references/rules/language.md
git commit -m "feat: 创建 language.md 规则模板"
```

---

### Task 3: 创建 code-usage.md 模板

**Files:**
- Create: `cadence-init/references/rules/code-usage.md`

- [ ] **Step 1: 创建文件**

从当前 CLAUDE.md 第 13-24 行提取内容，写入：

```markdown
## 代码使用规则

> **Skills 项目特殊规定**

- **非必要不编写代码** - 这是一个 Skills/Commands 配置项目，主要工作对象是 Markdown 文档、YAML 配置、JSON 数据等非代码文件。
- **必须说明理由** - 如果确实需要编写代码（如脚本、验证工具等），必须先向用户说明为什么必须使用代码，以及代码的作用和必要性。
- **优先替代方案** - 在决定编写代码前，必须先考虑是否可以通过纯文档方式、现有工具或配置文件完成相同目标。

**适用场景判断**：
- ✅ **允许**：文档编写、配置文件编辑、结构设计、方案规划、需求分析
- ⚠️ **需说明**：辅助脚本、验证工具、自动化处理（必须先说明必要性）
- ❌ **避免**：功能开发、业务逻辑实现、应用程序编写
```

- [ ] **Step 2: Commit**

```bash
git add cadence-init/references/rules/code-usage.md
git commit -m "feat: 创建 code-usage.md 规则模板"
```

---

### Task 4: 创建 document-storage.md 模板

**Files:**
- Create: `cadence-init/references/rules/document-storage.md`

> 这是从 CLAUDE.md 提取的最大文件（第 26-193 行），包含文档存储的完整规则。
> 注意：文档分类存储规范表需新增 `rules/` 条目。

- [ ] **Step 1: 创建文件**

从 CLAUDE.md 第 26-193 行原样复制内容，但需做以下修改：
- 在"文档分类存储规范"表中新增一行：`| **框架规则** | **`.claude/rules/`** | **框架内置规则文件（维护者管理）（🔴 新增）** |`，放在"个性化规则"行之前
- 标题从 `### 3. 文档存储规则` 改为 `## 文档存储规则`

- [ ] **Step 2: 验证内容完整性**

逐行对比新文件内容与 CLAUDE.md 第 26-193 行，确认：
- 文档分类存储规范表（含新增的 rules/ 条目）
- README 文档存储规则
- 文档命名规范（标准格式、Plan 格式、临时笔记格式）
- 命名规则表
- 版本号规则
- 所有检查清单
- 路径映射表
- 禁止行为列表

- [ ] **Step 3: Commit**

```bash
git add cadence-init/references/rules/document-storage.md
git commit -m "feat: 创建 document-storage.md 规则模板"
```

---

### Task 5: 创建 markdown-format.md 模板

**Files:**
- Create: `cadence-init/references/rules/markdown-format.md`

- [ ] **Step 1: 创建文件**

从 CLAUDE.md 第 195-207 行提取内容，写入：

````markdown
## Markdown 格式规则

- **严格注意 Markdown 格式** - 在编写包含代码块的文档时，如果出现代码块嵌套冲突，必须使用以下解决方案：
  - **外层代码块**：使用 4 个反引号 (````)
  - **内层代码块**：使用 3 个反引号 (``````)

**示例**：

```javascript
function example() {
  return "这是嵌套的代码示例";
}
```
````

- [ ] **Step 2: Commit**

```bash
git add cadence-init/references/rules/markdown-format.md
git commit -m "feat: 创建 markdown-format.md 规则模板"
```

---

### Task 6: 创建 serena-usage.md 模板

**Files:**
- Create: `cadence-init/references/rules/serena-usage.md`

- [ ] **Step 1: 创建文件**

从 CLAUDE.md 第 209-239 行提取内容，标题从 `### 5. Serena 使用规则` 改为 `## Serena 使用规则`，正文内容原样保留（包含正确/错误做法示例代码块）。注意文件内部包含 3 反引号代码块，这是正常的。

- [ ] **Step 2: Commit**

```bash
git add cadence-init/references/rules/serena-usage.md
git commit -m "feat: 创建 serena-usage.md 规则模板"
```

---

### Task 7: 创建 mcp-servers.md 模板

**Files:**
- Create: `cadence-init/references/rules/mcp-servers.md`

> 以 `cadence-init/commands/mcp-configuration.md` 中的 MCP 规则文本为主要来源（因为它比 CLAUDE.md 更完整，包含典型工作流、环境变量等内容）。
> 需要移除 command 特有的指令（如"用户确认流程"、"添加以下规则到 CLAUDE.md"等）。
> 需要合并 CLAUDE.md 第 241-448 行中的独有内容（如 API Key 安全提醒）。

- [ ] **Step 1: 创建文件**

以 `mcp-configuration.md` 中步骤 1 的 MCP 规则文本为基础，同时合并 CLAUDE.md 第 241-448 行的内容。标题设为 `## MCP Server 使用规则`。

**包含内容**：
- Time MCP（用途、触发场景、使用方式）
- Context7 MCP（用途、触发场景、使用方式、示例）
- Sequential Thinking MCP（用途、触发场景、使用方式）
- Serena MCP（用途、触发场景、常用命令、重要规则）
- 智普视觉理解 MCP（用途、触发场景、工具列表、使用规则、典型工作流）— 来自 mcp-configuration.md
- 智普联网搜索 MCP（用途、触发场景、工具列表、使用规则、典型工作流）— 来自 mcp-configuration.md
- 智普网页读取 MCP（用途、触发场景、工具列表、使用规则、典型工作流）— 来自 mcp-configuration.md
- 智普开源仓库 MCP（用途、触发场景、工具列表、使用规则、典型工作流）— 来自 mcp-configuration.md
- MiniMax Token Plan MCP（用途、触发场景、环境变量、使用规则、典型工作流）— 来自 mcp-configuration.md
- API Key 安全提醒 — 来自 CLAUDE.md

**移除内容**（command 特有指令，不属于规则文件）：
- "用户确认"流程描述
- "添加前必须询问用户"提示
- "添加以下规则到 CLAUDE.md"指令

- [ ] **Step 2: 验证内容完整性**

逐行对比确认所有 MCP 规则都已包含，无遗漏。

- [ ] **Step 3: Commit**

```bash
git add cadence-init/references/rules/mcp-servers.md
git commit -m "feat: 创建 mcp-servers.md 规则模板"
```

---

### Task 8: 创建 playwright.md 模板

**Files:**
- Create: `cadence-init/references/rules/playwright.md`

- [ ] **Step 1: 创建文件**

从 CLAUDE.md 第 489-618 行原样复制内容，标题从顶级 `## Playwright CLI 使用规则` 保持不变。内容包含：
- 用途
- 触发场景
- 常用命令（基础操作、会话管理、存储操作）
- 使用规则
- 典型工作流
- 重要规则

- [ ] **Step 2: Commit**

```bash
git add cadence-init/references/rules/playwright.md
git commit -m "feat: 创建 playwright.md 规则模板"
```

---

## Chunk 2: 修改 cadence-init commands

> 更新 rule-config.md、mcp-configuration.md、project-rules-examples.md 三个 command 文件。

### Task 9: 修改 rule-config.md

**Files:**
- Modify: `cadence-init/commands/rule-config.md`

> 这个文件当前直接将规则原文写入 CLAUDE.md，需改为：引用 references/rules/ 模板创建 rules/ 文件，CLAUDE.md 只写摘要引用。

- [ ] **Step 1: 修改概述**

将 `rule-config.md` 的"概述"段落更新为：

```markdown
## 概述

配置 Claude Code 的规则：创建 `.claude/rules/` 目录下的规则文件，在 CLAUDE.md 中添加摘要引用。
```

- [ ] **Step 2: 修改检查清单**

将检查清单更新为：

```markdown
## 检查清单

你必须为以下每个项目创建任务并按顺序完成：

1. **创建 rules 目录和规则文件** — 从 `references/rules/` 读取模板，创建到 `.claude/rules/`
2. **语言规则引用** — 在 CLAUDE.md 中添加语言规则摘要引用
3. **代码使用规则引用** — 在 CLAUDE.md 中添加代码使用规则摘要引用
4. **文档存储规则引用** — 在 CLAUDE.md 中添加文档存储规则摘要引用
5. **包管理器规则** — 前端使用 pnpm，Python 使用 uv
6. **技术栈检测** — 自动检测语言、测试/检查/格式化命令，需要用户确认
7. **目录结构创建** — 创建 `.claude/` 子目录
8. **Playwright Skills 规则配置** — 配置 Playwright CLI 的使用规则（可选）
```

- [ ] **Step 3: 修改处理流程**

将步骤 1-3（语言规则、文档存储规则、文档命名规则）替换为新的处理流程：

**步骤 1（新）：创建 rules 目录和规则文件**
- 创建 `.claude/rules/` 目录
- 从 `cadence-init/references/rules/` 复制模板文件到项目的 `.claude/rules/` 目录：
  - `language.md`、`code-usage.md`、`document-storage.md`、`markdown-format.md`、`serena-usage.md`

**步骤 2-4（新）：在 CLAUDE.md 中添加全部 8 个规则的摘要引用**

不再写入规则原文，改为只添加摘要引用行。CLAUDE.md 中需包含以下全部引用：

| 规则编号 | 摘要引用内容 |
|---------|------------|
| 1 | `### 1. 语言规则` + `- **必须使用中文回答** → 详见 .claude/rules/language.md` |
| 2 | `### 2. 代码使用规则` + `- **非必要不编写代码** → 详见 .claude/rules/code-usage.md` |
| 3 | `### 3. 文档存储规则` + `- **所有文档必须存放在 .claude 目录下** → 详见 .claude/rules/document-storage.md` |
| 4 | `### 4. Markdown 格式规则` + `- **代码块嵌套使用 4 反引号/3 反引号** → 详见 .claude/rules/markdown-format.md` |
| 5 | `### 5. Serena 使用规则` + `- **禁止分析 .git 目录** → 详见 .claude/rules/serena-usage.md` |
| 6 | `### 6. MCP Server 使用规则` + `- **各 MCP 工具的使用规范** → 详见 .claude/rules/mcp-servers.md`（由 mcp-configuration command 添加） |
| 7 | `### 7. 项目个性化规则（强制规则）` + 强制约束声明（由 project-rules-examples command 添加） |
| 8 | `### 8. Playwright CLI 使用规则` + `- **浏览器自动化工具规范** → 详见 .claude/rules/playwright.md`（步骤 8 添加） |

- [ ] **Step 4: 更新步骤 6（目录结构创建）**

在 mkdir 命令中增加 `rules` 目录：

```bash
mkdir -p .claude/{rules,prds,analysis-docs,docs,designs,designs-reviews,plans,readmes,modaos,models,architecture,notes,logs,reports,project-rules/examples}
```

- [ ] **Step 5: 更新步骤 7（Playwright）**

将 Playwright 规则的添加方式改为：

```markdown
### 8. Playwright Skills 规则配置

**创建规则文件**：将 `references/rules/playwright.md` 复制到 `.claude/rules/playwright.md`

**在 CLAUDE.md 中添加**：

```markdown
### 8. Playwright CLI 使用规则
- **浏览器自动化工具规范** → 详见 `.claude/rules/playwright.md`
```
```

- [ ] **Step 6: Commit**

```bash
git add cadence-init/commands/rule-config.md
git commit -m "feat: 更新 rule-config 使用 references/rules/ 模板"
```

---

### Task 10: 修改 mcp-configuration.md

**Files:**
- Modify: `cadence-init/commands/mcp-configuration.md`

> 这个文件当前将约 300 行 MCP 规则文本硬编码写入 CLAUDE.md，需改为创建 rules/mcp-servers.md 文件。

- [ ] **Step 1: 修改步骤 1 的标题和说明**

将 `### 1. MCP 使用规则配置` 下的 `**添加以下规则到 CLAUDE.md**：` 替换为：
- 标题保持 `### 1. MCP 使用规则配置`
- 说明改为：从 `references/rules/mcp-servers.md` 读取模板，写入 `.claude/rules/mcp-servers.md`
- 在 CLAUDE.md 中添加一行摘要引用：`- **各 MCP 工具的使用规范** → 详见 .claude/rules/mcp-servers.md`

- [ ] **Step 2: 删除步骤 1 中的硬编码规则文本**

删除步骤 1 中从 `#### Time MCP` 开始到 `使用 Git 命令获取版本信息` 结束的所有 MCP 规则原文（约 280 行）。这些内容已移到 `references/rules/mcp-servers.md` 中。

- [ ] **Step 3: 更新步骤 4 的智普/MiniMax 规则配置方式**

将"添加规则到 CLAUDE.md"的描述改为"追加到 `.claude/rules/mcp-servers.md` 文件末尾"。

- [ ] **Step 4: Commit**

```bash
git add cadence-init/commands/mcp-configuration.md
git commit -m "feat: 更新 mcp-configuration 使用 references/rules/ 模板"
```

---

### Task 11: 更新 references/project-rules/ 模板文件

**Files:**
- Modify: `cadence-init/references/project-rules/CLAUDE-RULE.md`
- Modify: `cadence-init/references/project-rules/README.md`

- [ ] **Step 1: 更新 CLAUDE-RULE.md**

将整个文件内容替换为新版本。新版本包含：
1. 强制约束声明（project-rules 是唯一合法目录，禁止修改 rules/）
2. 规则目录和使用方法说明
3. 示例规则（用 HTML 注释包裹的示例代码）
4. 说明文字

注意：新文件中包含嵌套的 markdown 代码示例（用 4 反引号包裹），使用 5 反引号作为最外层以避免解析歧义。

- [ ] **Step 2: 更新 project-rules/README.md**

在现有内容基础上增加以下章节：

```markdown
## 强制约束

> **🔴 以下规则必须遵守**

### 规则目录划分

| 目录 | 用途 | 管理者 |
|------|------|--------|
| `.claude/rules/` | 框架内置规则文件 | 框架维护者 |
| `.claude/project-rules/` | 用户自定义规则文件 | 用户 |

### 禁止行为

- ❌ **禁止**在 `.claude/rules/` 目录中添加用户自定义规则
- ❌ **禁止**直接修改 `.claude/rules/` 目录下的框架内置规则文件
- ❌ **禁止**在项目根目录创建规则文件

### 正确做法

- ✅ 用户自定义规则放在 `.claude/project-rules/` 目录
- ✅ 从 `examples/` 目录复制模板并修改
- ✅ 在 `CLAUDE.md` 中添加规则引用以启用
```

- [ ] **Step 3: Commit**

```bash
git add cadence-init/references/project-rules/CLAUDE-RULE.md cadence-init/references/project-rules/README.md
git commit -m "feat: 更新 project-rules 模板，增加强制约束"
```

---

### Task 12: 修改 project-rules-examples.md

**Files:**
- Modify: `cadence-init/commands/project-rules-examples.md`

- [ ] **Step 1: 更新步骤 6 的引用方式**

将步骤 6 "添加 CLAUDE.md 规则"中的内容修改为：

```markdown
### 6. 添加 CLAUDE.md 规则引用

在项目的 `CLAUDE.md` 文件中添加个性化规则引用。

**目标文件**：项目根目录的 `CLAUDE.md`

**添加位置**：在 CLAUDE.md 的"强制规则"章节末尾

**添加内容**：`references/project-rules/CLAUDE-RULE.md`（已更新为强制约束 + 摘要引用格式）

**注意事项**：
- 确保 CLAUDE.md 中已有 `### 7. 项目个性化规则（强制规则）` 摘要引用
- `project-rules/` 是用户自定义规则的唯一合法目录
- 禁止将用户自定义规则写入 `.claude/rules/` 目录
```

- [ ] **Step 2: Commit**

```bash
git add cadence-init/commands/project-rules-examples.md
git commit -m "feat: 更新 project-rules-examples 使用新的引用方式"
```

---

## Chunk 3: 重构本项目 CLAUDE.md 并审查其他文件

> 将当前项目的 CLAUDE.md 从 ~620 行精简为 ~120 行，并创建 .claude/rules/ 目录和规则文件。审查其他 commands/skills。

### Task 13: 创建本项目的 .claude/rules/ 目录和规则文件

> **前置条件**：Chunk 1 全部任务（Task 1-8）已完成，`cadence-init/references/rules/` 目录下已有全部模板文件。

**Files:**
- Create: `.claude/rules/README.md`
- Create: `.claude/rules/language.md`
- Create: `.claude/rules/code-usage.md`
- Create: `.claude/rules/document-storage.md`
- Create: `.claude/rules/markdown-format.md`
- Create: `.claude/rules/serena-usage.md`
- Create: `.claude/rules/mcp-servers.md`
- Create: `.claude/rules/playwright.md`

- [ ] **Step 1: 创建 rules 目录**

```bash
mkdir -p .claude/rules
```

- [ ] **Step 2: 将 cadence-init/references/rules/ 下的模板文件复制到 .claude/rules/**

```bash
cp cadence-init/references/rules/*.md .claude/rules/
```

- [ ] **Step 3: 验证所有文件已创建**

```bash
ls -la .claude/rules/
# 应看到 8 个文件：README.md + 7 个规则文件
```

- [ ] **Step 4: Commit**

```bash
git add .claude/rules/
git commit -m "feat: 创建 .claude/rules/ 目录和规则文件"
```

---

### Task 14: 重构 CLAUDE.md

**Files:**
- Modify: `CLAUDE.md`

> 这是最关键的一步。将 ~620 行的 CLAUDE.md 精简为 ~120 行。

- [ ] **Step 1: 备份原 CLAUDE.md**

```bash
cp CLAUDE.md CLAUDE.md.bak
```

- [ ] **Step 2: 重写 CLAUDE.md**

将 CLAUDE.md 的全部内容替换为设计文档第 3 节中定义的结构。保留"项目配置"区域的包管理器和技术栈信息。

精简后的 CLAUDE.md 应包含：

1. 标题和简介（2 行）
2. 强制规则标题 + 提示块（4 行）
3. 8 个规则的摘要引用（每个 2-3 行，共 ~20 行）
4. 项目配置区域（包管理器 + 技术栈，~15 行）
5. 项目信息（3 行）

总计约 50-60 行（本项目无包管理器和技术栈配置，可省略项目配置区域）。

- [ ] **Step 3: 验证重构结果**

```bash
wc -l CLAUDE.md
# 应小于 80 行
```

逐个检查每个引用路径指向的文件是否存在：
```bash
for f in README language code-usage document-storage markdown-format serena-usage mcp-servers playwright; do
  test -f ".claude/rules/$f.md" && echo "✅ $f.md" || echo "❌ $f.md MISSING"
done
```

- [ ] **Step 4: 删除备份文件**

```bash
rm CLAUDE.md.bak
```

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md
git commit -m "refactor: 精简 CLAUDE.md 为摘要+引用格式，详细规则移至 .claude/rules/"
```

---

### Task 15: 审查其他 commands/skills

**Files:**
- Read: `cadence-init/commands/pre-check.md`
- Read: `cadence-init/commands/project-analysis.md`
- Read: `cadence-init/commands/skill-create.md`
- Read: `cadence-workflow/commands/*.md`（所有文件）

- [ ] **Step 1: 审查 pre-check.md**

读取文件，检查是否有：
- 硬编码的规则路径引用（如 `.claude/project-rules/`）→ 无需修改，它只做环境检查
- 规则创建逻辑 → 无

结论：**pre-check.md 无需修改**。它只检查工具安装情况，不涉及规则路径。

- [ ] **Step 2: 审查 project-analysis.md**

读取文件，检查是否有：
- 规则路径引用 → 无
- 规则创建逻辑 → 无

结论：**project-analysis.md 无需修改**。它只生成分析报告，不涉及规则。

- [ ] **Step 3: 审查 skill-create.md**

读取文件，检查是否有：
- 规则路径引用 → 无，它操作 `.claude/skills/` 目录
- 规则创建逻辑 → 无

结论：**skill-create.md 无需修改**。

- [ ] **Step 4: 批量审查 cadence-workflow/commands/**

对每个 workflow command 文件进行快速检查，搜索以下关键词：
- `.claude/rules`
- `.claude/project-rules`
- `CLAUDE.md`（写入操作）

```bash
grep -l "rules\|CLAUDE\.md" cadence-workflow/commands/*.md || echo "No matches found"
```

如果有匹配，逐个读取确认是否需要修改。

预期结论：**workflow commands 不涉及规则创建，无需修改**。

- [ ] **Step 5: 记录审查结果并 Commit（如有修改）**

如果审查发现需要修改的文件，逐个 add 并 commit：

```bash
# 只 add 审查中修改的文件（替换为实际文件名）
git add <specific-files>
git commit -m "fix: 更新审查发现的相关 commands 文件"
```

如果无需修改，跳过此步骤。

---

### Task 16: 最终验证和提交

- [ ] **Step 1: 验证目录结构**

```bash
# 验证 references/rules/ 模板
ls cadence-init/references/rules/
# 应看到 8 个文件

# 验证 .claude/rules/ 规则文件
ls .claude/rules/
# 应看到 8 个文件

# 验证 .claude/project-rules/ 保持不变
ls .claude/project-rules/
# 应看到 README.md 和 examples/ 目录
```

- [ ] **Step 2: 验证 CLAUDE.md 行数**

```bash
wc -l CLAUDE.md
# 应小于 80 行
```

- [ ] **Step 3: 验证引用完整性**

检查 CLAUDE.md 中每个引用的路径都有对应文件：

```bash
grep -o '\.claude/rules/[^`]*' CLAUDE.md | while read f; do
  test -f "$f" && echo "✅ $f" || echo "❌ $f MISSING"
done
```

- [ ] **Step 4: 验收确认**

确认设计文档中的 7 条验收标准：

1. ✅ CLAUDE.md 精简至 ~120 行以内
2. ✅ .claude/rules/ 下有 7 个规则文件 + 1 个 README
3. ✅ 拆分后文件内容与原 CLAUDE.md 对应部分一致
4. ✅ cadence-init/references/rules/ 有对应的 7+1 个模板文件
5. ✅ project-rules/ 的 README 和模板已更新含强制约束
6. ✅ rule-config.md、mcp-configuration.md、project-rules-examples.md 已更新
7. ✅ 所有相关 commands/skills 已审查

- [ ] **Step 5: 最终 Commit**

如果所有修改已在之前提交，此步骤可跳过。如有遗漏：

```bash
git add <specific-files>
git commit -m "feat: 完成规则目录重构 - CLAUDE.md 精简、rules 目录创建、commands 更新"
```

---

## 后续任务（本次范围外，必须尽快完成）

> **🔴 依赖风险**：install-offline.sh 和 install-offline.bat 的复制列表必须增加 `rules/` 目录，否则离线安装后 CLAUDE.md 的引用将全部失效。

### 后续 Task A: 更新 install-offline.sh

在 `install-offline.sh` 的文件复制逻辑中增加 `cadence-init/references/rules/` → `.claude/rules/` 的复制步骤。

### 后续 Task B: 更新 install-offline.bat

在 `install-offline.bat` 中做同样的修改。

### 后续 Task C: 更新 readmes/ 用户文档

更新 `readmes/` 目录下的用户文档，反映新的 rules 目录结构。

## Markdown 格式规则

- **严格注意 Markdown 格式** - 在编写包含代码块的文档时，如果出现代码块嵌套冲突，必须使用以下解决方案：
  - **外层代码块**：使用 4 个反引号 (````)
  - **内层代码块**：使用 3 个反引号 (```)

**示例**：

```javascript
function example() {
  return "这是嵌套的代码示例";
}
```